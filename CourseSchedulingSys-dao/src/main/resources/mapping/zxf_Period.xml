<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
 <mapper namespace="com.accp.course.dao.zxf.PeriodQueryall">
 	<select id="QueryAll" resultType="com.accp.course.pojo.Period">
 		SELECT * FROM period 
 	</select>
 	
 	<delete id="Delete">
 		delete from  `courseschedulingsys`.`period` where `id` =#{id}
 	</delete>
 </mapper>